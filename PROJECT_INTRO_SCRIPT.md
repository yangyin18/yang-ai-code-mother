# 项目介绍话术（口语背诵脚本）

> 用法：把下面的话当"台词"来背，但别当稿子念——括号里是给你的提示（停顿、强调、动作），灰色部分是可以根据现场换掉的。一共三档时长，面试/汇报选"深度版"。

---

## 一、30 秒电梯版（一句话介绍 / 陌生人破冰）

> **适用**：被问"你最近在做什么"、"你这项目是干嘛的"。

"我最近做了一个** AI 零代码应用平台**。就是用户只需要输入一句话需求——比如'给我做一个带购物车的电商落地页'——系统会自动调用大模型生成代码，而且是**真正能跑、能部署上线**的网页应用。

支持三种模式：简单的单页 HTML、多文件页面、还有**完整可运行的 Vue 工程**。生成过程是**流式的**，用户能看到代码一个一个字打出来；生成完还能**继续对话改**、**可视化点选编辑**，最后**一键部署到 nginx**，用链接就能访问。

整个项目是前后端分离的：后端 Spring Boot + MyBatis-Flex + langchain4j 接 DeepSeek 大模型，前端 Vue 3 全家桶。核心的代码生成管线、AI 服务装配、权限体系、流式通信链路都是我做的。"

⏱️ 约 30 秒。语速放慢，重点词"**能跑、能部署**""**对话改**""**一键部署**"稍微重读。

---

## 二、3 分钟标准版（项目负责人 / 组会汇报）

> **适用**：让你正式介绍项目，但时间有限。顺序是"总 → 流程 → 亮点 → 一句话收尾"。

### 第 1 段：一句话定位

"我先用一句话概括这个项目：**它是一个用一句话需求，自动生成并上线完整网页应用的平台**，目标是让不会写代码的人也能快速做出自己的网站。"

### 第 2 段：它解决什么问题

"这个问题的背景是——现在普通人想做一个小网站，要么不会写代码，要么用模板工具但很受限。我们就想让 AI 直接当'全栈工程师'：你说需求，它写代码，而且我们保证写出来的东西**不只是给你看看代码，而是真能跑、真能上线**。"

### 第 3 段：核心功能串一遍（跟着用户视角走）

"整个流程是这样：

用户在首页输入一句话需求，选择模式——快速开发或者深度开发。

选**快速开发**，系统调大模型，**通过 SSE 流式**把代码一行一行推给前端，用户能看到实时预览，就像看着别人现场写代码。

选**深度开发**，AI 会作为一个 agent，通过工具调用**逐个生成几十个 Vue 工程文件**——package.json、vite 配置、组件、路由，一个不少。

生成之后，用户可以继续对话改（比如'把标题改成蓝色'），AI 会重新生成并**自动重新部署**；也可以**可视化点选**——直接点在网页上选一个元素，改颜色、改文字、改边距。

最后点**一键部署**，项目会被构建、发布到 nginx，用户拿到一个公网链接直接访问。"

### 第 4 段：技术架构一句话

"技术栈上，后端是 Spring Boot 4 + Java 21，ORM 用 MyBatis-Flex，AI 层用 langchain4j 接 DeepSeek 大模型，Redis 做对话记忆，MySQL 做持久化；前端是 Vue 3 + TypeScript + Vite + Pinia。前后端通过 SSE 做流式通信。"

### 第 5 段：最亮眼的两个设计（埋钩子，等对方追问）

"这个项目里我自己最满意的是两个设计：

一个是 **AI 用工具调用生成完整工程**——不是让模型一次性吐一大段代码，而是暴露一个 `writeFile` 工具，让模型像个工程师一样一个文件一个文件地写，这样工程的结构和质量都可控。

另一个是**确定性兜底**——因为大模型经常漏文件、引用不存在的文件，我用脚手架自动补齐关键文件、给缺失引用补桩，保证生成的东西**一定能构建、能跑、不白屏**。这个取舍是：宁可用确定性的代码去兜底，也不把问题喂回模型多轮重试。"

### 第 6 段：收尾

"整个项目从后端到前端、从生成到部署，是我一个人从零搭起来的，核心的生成管线、AI 接入、权限、流式通信都是我自己实现的。"

⏱️ 约 3 分钟。这一段里"SSE 流式"、"agent 工具调用"、"确定性兜底"是关键词，讲的时候可以稍作停顿，给对方提问空间。

---

## 三、10 分钟深度版（面试完整介绍）

> **适用**：面试官让你"详细介绍一下项目"。这一版把 3 分钟版展开，每一段都预埋了可以深挖的点。

### 开场（30 秒）

"谢谢，我详细介绍一下。这个项目叫 AI 零代码应用平台，是我一个人从后端到前端完整实现的。它的定位是：**输入一句话需求，自动生成并上线一个真正能运行的网页应用**。下面我按'整体架构 → 核心流程 → 我做的关键设计 → 遇到的难点 → 还可以怎么改进'来讲。"

### 第一块：整体架构（1 分半）

"先说架构。整体是前后端分离：

**前端**用 Vue 3 + TypeScript + Vite，状态管理用 Pinia，UI 用 Ant Design Vue。大概有十个页面：首页、生成页、对话页、Vue 工程页、我的对话，加上两个管理员页面。

**后端**是 Spring Boot 4，Java 21。分了几层：
- Controller 层负责接请求和 SSE 流式接口；
- Service 层管业务逻辑和数据权限；
- 中间有一个**核心门面类**，叫 CodeGenFacade，它把'生成代码'这个复杂流程统一编排起来；
- 再往下是 AI 层，用 langchain4j 接 DeepSeek 大模型；
- 然后是一套**代码处理管线**：解析器把模型的输出拆成结构化文件，净化器做安全处理，保存器落盘；
- 存储用 MySQL 做主数据，Redis 做对话记忆缓存。

**通信上**，普通接口走 JSON，生成和部署这种长耗时操作走 **SSE 流式**——因为前端要实时看到代码逐字输出和构建进度。"

（💡 这里面试官大概率会打断问"SSE 为什么不用 WebSocket""为什么用 SSE"，这正是我埋的点。）

### 第二块：三种生成模式 & 核心流程（3 分钟）

"项目支持三种生成模式，我用一个枚举区分，每种模式流程差异很大，所以我用门面统一对外：

**第一种，原生 HTML 模式**：单文件。用户需求会跟这个应用的 initPrompt（初始化的基础指令）拼在一起，发给模型，模型按要求输出严格 JSON，解析出 htmlCode 落盘。

**第二种，多文件模式**：index.html、style.css、script.js 三个文件分开生成。

**第三种，Vue 工程模式**，这是最复杂的。它不是一个'生成一次'的流程，而是**把 AI 变成一个 agent**：我定义了 `AiVueProjectService` 接口，暴露 `writeFile` 这个工具方法给模型调用。系统提示词要求模型先输出一个生成计划，然后一个文件一个文件地调 writeFile 写入——package.json、vite 配置、入口、组件、路由。每写一个文件，后端就通过 SSE 推一个 file 事件，前端终端界面显示'✓ 已生成 src/App.vue'。

写完之后，我会做一个**确定性加固**（hardenVueProject）：用脚手架把缺失的关键文件补齐、把引用不存在的模块补桩，保证工程能构建。

**生成和部署是分开的**。部署时，普通模式直接把源码拷贝到 nginx 站点目录；Vue 模式会先在服务器上跑 npm install 和 npm build，构建出 dist 再发布。部署地址用 deployKey 来组织，一个应用一个稳定链接。"

### 第三块：我做的关键设计（每个都是可以单独深挖的钩子，3 分钟）

"这里面有几个我觉得值得说的设计：

**第一个，流式解析。** 前端用 fetch 拿 ReadableStream 手动解析 SSE，而不是用 EventSource——因为 EventSource 只支持 GET，而我们的生成接口要 POST。这里有个很细的坑：SSE 的 data 字段不能 trim，因为空格本身就是代码的一部分。我处理完才发现，如果不注意，HTML 开头的空格会被吃掉。

**第二个，三级降级解析器。** 模型输出不稳定，所以我的解析器是逐级降级的：先尝试解析严格 JSON，不行就解析 Markdown 代码块，再不行就当裸 HTML。而且文件名有三级优先级：文件标签、语言标签、出现顺序。这样不管模型输出什么乱七八糟的格式，我们都能把代码救回来。

**第三个，安全净化。** 模型生成的代码可能带外链、跳转脚本，我在提示词里约束的同时，代码层再净化一遍——移除 a 标签、base、外部 url、location 跳转。双保险，保证应用自包含、不会跳去外部平台。

**第四个，对话记忆的双存储。** AI 对话需要上下文，我用 Redis 做短缓存，key 是每个应用一个，TTL 一小时、每次写入续期；但持久主存储是 MySQL 的聊天记录表，缓存过期了可以从库里重建最近十条。这样性能和数据安全都照顾到。

**第五个，权限体系。** 我用了两层：注解加 AOP 切面做登录和角色校验，比如管理员接口标 @AuthCheck(role="admin")；Service 层再做数据级校验——这个应用是不是你的、能不能看、能不能改，都在业务里判断。注解管'能不能进'，Service 管'能不能碰这条数据'。"

### 第四块：遇到的难点（2 分钟）——真实感最重要

"我讲几个实际踩过的坑，都是不看源码根本发现不了的那种：

**第一个，线程上下文类加载器。** langchain4j 的 AI 流式回调是跑在它自己的线程上的，那个线程的 ClassLoader 读不到我们 Spring 的实体类。MyBatis-Flex 用 lambda 方法引用查数据库的时候，直接抛 ClassNotFoundException。我最后改成用字符串列名 `new QueryColumn("appId")` 绕过。这个问题非常隐蔽，排查了很久。

**第二个，大模型首 token 太慢。** 我们用的 DeepSeek 是推理模型，生成代码前会先输出大段思考过程，导致用户要干等接近一分钟才看到第一个字。我通过 customParameters 把 thinking 禁用掉，首 token 从 95 秒降到 1.3 秒，总耗时从 95 秒降到 10 秒。这是数据驱动的一个优化。

**第三个，前端精度丢失。** 数据库主键是雪花 ID，64 位 Long，直接返回给前端，JSON.parse 之后后几位就丢了。我用 Jackson 全局把 Long 序列化成字符串解决。这是前后端协作很经典的坑。

**第四个，Vue 工程跑不起来。** 大模型生成 Vue 工程经常漏文件、引用了不存在的模块。我最初在部署时才补桩，但补出来是空组件，页面白屏。后来改成生成期就做确定性兜底，而且把空桩升级成'可见占位'——用户看到的是'待完善模块'的提示，而不是白屏。"

### 第五块：改进方向（1 分钟）——主动暴露 + 给出方案

"如果继续做，我最想改进的有几处：

第一，**密码加密**现在还是 MD5 加固定盐，应该升级成 bcrypt 或者 Argon2。

第二，**会话**目前是 Tomcat 内存 Session，单体没问题，但上多实例就得引入 spring-session 做 Redis 会话共享。

第三，**生成任务**现在完全依赖 SSE 长连接，如果做真正的生产环境，应该改成异步任务 + 消息队列，生成完再通知。

第四，前端目前**没有全局路由守卫**，登录校验是每个页面自己做的，应该抽成一个统一的 beforeEach。

第五，数据库的 user 表**建表脚本缺失**，环境初始化会踩坑，应该补上。"

### 收尾（30 秒）

"总结一下：这个项目我完整经历了从需求、架构设计、前后端实现到部署上线，核心难点集中在**AI 输出的不可控性**上，我所有的设计——流式、解析器、净化、加固——都是在和这种不可控性做对抗。这是我的一个很完整的全栈作品。"

---

## 四、打断应对（现场被问到的临场话术）

### 问："这个项目是你一个人做的？" / "你具体负责哪部分？"

"对，从架构到代码都是我一个人。具体到核心部分：**后端**我写了整个代码生成管线——门面编排、解析器、保存器、净化器；**AI 层**的模型装配、Vue 工具调用、Redis 记忆；**权限体系**的注解和切面；**前端**的生成页、对话页、SSE 解析和可视化编辑。基本上除了现成组件库，业务代码全是我写的。"

### 问："这是不是就是套壳调 API？"（试探性/挑战性问题）

"调 API 确实是最小的一步，这个项目的难度不在'调大模型'，而在**怎么把模型不稳定的输出变成稳定可用的产品**。举个例子：模型吐回来的可能是 JSON、可能是 Markdown、可能带一堆废话，我写了三级降级解析器去救；它可能生成一个缺文件、引用了不存在模块的 Vue 工程，我写了确定性的脚手架和补桩去兜底；它可能首 token 要等 95 秒，我通过禁用思考优化到 1.3 秒。真正的工程工作量都在这些地方，套壳调 API 是做不到这些的。"

### 问："为什么不用现成的 AI 编程工具/Coze 这类平台？"

"如果用现成的平台，就没有自己实现核心链路的机会了。我做这个项目本质上是在**理解并复现一条 AI 生成应用的生产线**——从模型调用、流式传输、输出解析、安全净化、到构建部署。这个过程让我把 SSE、agent 工具调用、前端流式渲染、构建链路全串起来了。工具平台是实现手段，理解和亲手实现是目的。"

### 问："项目有什么难点？"（对方没指定，主动选最强的讲）

"最大的难点是**让 AI 的输出变得可控和可用**。因为大模型的输出天然不可控，而我这个产品要求它产出的东西必须能运行、能部署。我围绕这个做了四件事：解析器兜格式、净化器兜安全、脚手架兜缺失、Token 预算兜超长。另外还有一个很磨人的点：SSE 流式和线程上下文类加载器这两个问题，都是查了很久才发现根因的。"

---

## 五、话术技巧（通用）

1. **结论先行**：永远先给一句话定位，再展开细节。没有人喜欢听你从需求文档开始讲。
2. **预埋钩子**：讲到"SSE 流式""agent 工具调用""确定性兜底"时稍作停顿、不展开，等对方问。被追问 = 他在认真听 = 你的主场。
3. **用对比讲亮点**："不是一次输出，而是一个文件一个文件地写"、"不是把问题喂回模型，而是用确定性代码兜底" —— 对比句最好记也最有力。
4. **真实感 > 完美感**：主动讲踩过的坑（TCCL、精度丢失、首 token 慢），比只报喜更有说服力。
5. **控制在对话感**：语速中等偏慢，关键词重读，段与段之间用"那""还有一个"自然衔接，别像念稿。
6. **准备一张 A4 纸**：把架构图画一遍、把三条生成流程写一遍，讲到哪都心里有数。

---

*脚本完。建议先背 30 秒版建立信心，再背 3 分钟版，最后吃透 10 分钟版——三档覆盖所有场合。*
